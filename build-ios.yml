name: Build XGO iOS App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Umožňuje ruční spuštění

jobs:
  build:
    runs-on: macos-14  # macOS Sonoma s Apple Silicon (M1)
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Ruby (pro CocoaPods)
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'
        bundler-cache: true

    - name: Install CocoaPods
      run: |
        gem install cocoapods
        pod install

    - name: Zobrazení dostupných Xcode verzí
      run: ls /Applications/ | grep Xcode

    - name: Výběr Xcode
      run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer

    - name: Build aplikace (bez podpisu - pro testování)
      run: |
        xcodebuild \
          -workspace xgo.xcworkspace \
          -scheme xgo \
          -sdk iphonesimulator \
          -configuration Release \
          -derivedDataPath build \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          build

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: xgo-ios-build
        path: build/Build/Products/Release-iphonesimulator/
        retention-days: 30

  # ============================================
  # VOLITELNÉ: Build podepsaného IPA souboru
  # Pro toto potřebujete Apple Developer účet
  # a nastavit GitHub Secrets (viz README)
  # ============================================
  # build-signed:
  #   runs-on: macos-14
  #   steps:
  #   - uses: actions/checkout@v4
  #   
  #   - name: Install CocoaPods
  #     run: |
  #       gem install cocoapods
  #       pod install
  #   
  #   - name: Import certificate
  #     env:
  #       BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
  #       P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
  #       KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
  #     run: |
  #       CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
  #       KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
  #       echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
  #       security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
  #       security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
  #       security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
  #       security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
  #       security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
  #       security list-keychain -d user -s $KEYCHAIN_PATH
  #   
  #   - name: Build & Archive
  #     run: |
  #       xcodebuild \
  #         -workspace xgo.xcworkspace \
  #         -scheme xgo \
  #         -sdk iphoneos \
  #         -configuration Release \
  #         -archivePath build/xgo.xcarchive \
  #         archive
  #   
  #   - name: Export IPA
  #     run: |
  #       xcodebuild -exportArchive \
  #         -archivePath build/xgo.xcarchive \
  #         -exportOptionsPlist ExportOptions.plist \
  #         -exportPath build/ipa
  #   
  #   - name: Upload IPA
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: xgo-ios-ipa
  #       path: build/ipa/*.ipa
